import React from 'react';

import Container from './styles';

const Header = (): React.ReactElement => {
  return (
    <Container>
      <img src="/Star_Wars_logo.svg" alt="StarWars" />
    </Container>
  );
};

export default Header;
